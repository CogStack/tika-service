package tika.server;

public class ServerConfig {
    int numTikaInstances;   // number of tika instances that cna be run in parallel
    int portNumber;         // this can be handled automatically in app properties
}
